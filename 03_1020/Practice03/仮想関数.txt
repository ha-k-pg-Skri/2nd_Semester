virtual (なにかしらの関数)

継承先に同じ関数があればそちらを優先して呼び出す
＝＞処理を上書きできる
関数のオーバライド

引数がちがったものを呼び出すとき
オーバーロード

virtalのような同じ引数のものを使うときは
オーバーライド

virtual ~Base()
継承する可能性のあるクラスのデストラクタには必ず
virtualを付けなければならない

Base* ptr<= new Player

BaseクラスのポインタはBaseクラスの中しか見れない

仮想関数を呼び出す

継承することが確実なとき
vrtualをクラスの中のデストラクタにつけないとnew したときに確保したメモリが残る


純粋仮想関数＝＞　virtual 関数の宣言＝ 0;

後ろに0を付けると純粋仮想関数になる
・Baseに実体は必要ない
・Virtualとしｔｒ継承先の関数を呼び出したい
